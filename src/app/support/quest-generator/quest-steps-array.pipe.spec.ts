import { QuestStepsArrayPipe } from './quest-steps-array.pipe';

describe('QuestStepsArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestStepsArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
